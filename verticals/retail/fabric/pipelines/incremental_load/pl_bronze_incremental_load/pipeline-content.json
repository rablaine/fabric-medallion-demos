{
  "properties": {
    "description": "Bronze incremental load. Same structure as pl_bronze_initial_load (the underlying notebooks are idempotent + merge-on-key, so re-running is the incremental path). Intended to be invoked from a daily/hourly schedule -- or from pl_incremental_load -- after the initial backfill is complete.",
    "activities": [
      {
        "name": "Weather catchup",
        "description": "Runs pl_bronze_weather_ingest. The ingest_weather notebook merges on (date, store_id) so re-running picks up only new days since the last run.",
        "type": "ExecutePipeline",
        "dependsOn": [],
        "policy": {
          "secureInput": false
        },
        "typeProperties": {
          "pipeline": {
            "referenceName": "__WEATHER_PIPELINE_ID__",
            "type": "PipelineReference"
          },
          "waitOnCompletion": true,
          "parameters": {},
          "workspaceId": "__BRONZE_WORKSPACE_ID__"
        }
      }
    ]
  }
}
